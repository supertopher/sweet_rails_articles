<article>
  <h1><%= @article.category.description %></h1>
  <h3><%= @article.title %></h3>
  <p><%= @article.tags.map{|this| this.description}.join(", ").chomp(",") %></p>
  <hr/>
  <p><%= @article.description %></p>
</article>
